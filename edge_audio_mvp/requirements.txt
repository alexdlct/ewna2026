# --- training / evaluation on the development PC ---
tensorflow>=2.20        # Keras 3 + TFLite converter
numpy>=1.26
soundfile>=0.12
av>=12                  # decodes .m4a / .aac phone voice memos that libsndfile can't
scikit-learn>=1.3
matplotlib>=3.8

# --- inference on the UNO Q (Linux side) ---
# numpy soundfile sounddevice          (sudo apt install libportaudio2 first)
# tflite-runtime  or  ai-edge-litert   (whichever has a wheel for the board's Python)
# pyserial                             only for --port bench demos with a UNO R4
